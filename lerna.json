{
  "version": "2.4.0",
  "packages": ["packages/*"],
  "command": {
    "publish": {
      "ignoreChanges": ["*.md", "*.txt"],
      "registry": "https://registry.npmjs.org/"
    },
    "version": {
      "allowBranch": ["release/*", "develop"]
    }
  }
}
